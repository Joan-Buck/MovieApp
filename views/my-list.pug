extends layout

append head
//-   script(src="/javascripts/reviews.js" type="module" defer)

block content
    button(type="submit")
        a(href='') New List
    each list in lists
        div.listCard= list.name
