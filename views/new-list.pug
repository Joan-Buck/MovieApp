extends layout

append head
//-   script(src="/javascripts/reviews.js" type="module" defer)

block content
    form(action="/my-lists" method="post")
        input(type="hidden", name="_csrf" value=csrfToken)
        //- label(for="name") Name your new list here
        input(type="text" name="name" placeholder="Name your new list here")
        button(class='listNameButton') Create New List
